using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationTVPrefab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(0.317f, -0.183f, 0.066f);
        transform.rotation = new Quaternion(0.000f, 0.0f, 0.000f, 1.0f);
        transform.localScale = new Vector3(1.255f, 1.255f, 1.255f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
