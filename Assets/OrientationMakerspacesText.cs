using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationMakerspacesText : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-1.457f, 0.272f, -0.528f);
        transform.rotation = new Quaternion(0.000f, -0.713f, 0.000f, 1.0f);
        transform.localScale = new Vector3(0.098f, 0.098f, 0.098f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
