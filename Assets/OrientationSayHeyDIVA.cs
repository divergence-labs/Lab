using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationSayHeyDIVA : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(0.854f, 0.912f, -0.263f);
        transform.rotation = new Quaternion(0.0f, 1.0f, 0.0f, 0.0f);
        transform.localScale = new Vector3(0.054f, 0.054f, 0.054f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
