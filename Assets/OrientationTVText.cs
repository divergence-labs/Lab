using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationTVText : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(0.482f, 0.356f, 0.015f);
        transform.rotation = new Quaternion(-0.003f, -0.593f, 0.02f, -0.805f);
        transform.localScale = new Vector3(0.056f, 0.056f, 0.056f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
