using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationMeetingOwl : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-0.268f, 0.303f, -0.046f);
        transform.rotation = new Quaternion(0.101f, -0.893f, -0.007f, 0.439f);
        transform.localScale = new Vector3(0.073f, 0.073f, 0.073f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
