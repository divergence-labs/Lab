using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationMakersSpace : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-1.336f, 0.363f, -0.525f);
        transform.rotation = new Quaternion(0.000f, 0.0f, 0.000f, 1.0f);
        transform.localScale = new Vector3(0.091f, 0.091f, 0.091f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
