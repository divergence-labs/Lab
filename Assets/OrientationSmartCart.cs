using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationSmartCart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-0.303f, 1.620f, 0.078f);
        transform.rotation = new Quaternion(0.0f, 0.536f, 0.0f, 0.844f);
        transform.localScale = new Vector3(0.155f, 0.155f, 0.155f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
