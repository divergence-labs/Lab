using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationWallText : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(0.796f, 0.362f, -0.550f);
        transform.rotation = new Quaternion(-0.051f, 0.699f, 0.042f, 0.712f);
        transform.localScale = new Vector3(0.084f, 0.084f, 0.084f);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
