using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationIOTRoomText : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-0.218f, 0.572f, -0.886f);
        transform.rotation = new Quaternion(0.0f, -0.124f, 0.0f, 0.992f);
        transform.localScale = new Vector3(0.068f, 0.068f, 0.068f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
