using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientationAIRoomText : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(-1.047f, 0.639f, -0.801f);
        transform.rotation = new Quaternion(-0.003f, -0.131f, -0.001f, 0.991f);
        transform.localScale = new Vector3(0.050f, 0.050f, 0.050f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
